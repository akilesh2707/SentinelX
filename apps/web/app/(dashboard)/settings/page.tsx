import SettingsPage from "../../../src/features/settings/settings-page";

export const metadata = {
    title: "Settings | SentinelX",
    description: "SentinelX platform settings and preferences"
};

export default function Page() {
    return <SettingsPage />;
}
